import { env } from "cloudflare:workers";import { ensureAdminSchema } from "../../../lib/admin-db";export const dynamic="force-dynamic";export async function GET(){await ensureAdminSchema();const announcement=await env.DB.prepare("SELECT id,content,created_at createdAt FROM announcements WHERE active=1 ORDER BY id DESC LIMIT 1").first();return Response.json({announcement:announcement||null})}
